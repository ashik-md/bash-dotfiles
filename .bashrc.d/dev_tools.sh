# MongoDB Alias
alias mongod-start='mongod --dbpath ~/data/db'  # Start MongoDB server

# React Native Alias
alias rn='react-native'  # Shortcut for react-native commands

# Yarn Aliases
alias y='yarn'           # Shortcut for yarn
alias ya='yarn add'      # Add a package
alias yad='yarn add --dev'  # Add a dev dependency
alias yr='yarn run'      # Run a script
alias ys='yarn start'    # Start a yarn project

# Expo CLI Alias
alias expo='expo-cli'    # Shortcut for expo commands

# Kubernetes Aliases
alias k='kubectl'        # Shortcut for kubectl
alias kg='kubectl get'   # Get resources
alias kd='kubectl delete'  # Delete resources
